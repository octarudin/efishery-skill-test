#ifndef CommonVariables_h
#define CommonVariables_h

const long interval = 100;            // interval at which to get data (milliseconds)
unsigned long previousMillis = 0;     // will store last time sensor was updated
unsigned char looping = 0;

#endif
